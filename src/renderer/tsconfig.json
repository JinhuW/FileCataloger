{
  // TypeScript config for RENDERER PROCESS (Electron frontend/UI)
  // This process runs in Chromium and renders the user interface
  //
  // ⚠️ IMPORTANT: This config is for React components and browser code
  // DO NOT merge this with tsconfig.main.json!
  //
  // What's different here:
  // - HAS DOM types (window, document, etc.) for React components
  // - NO direct Electron main process APIs (would be security risk)
  // - Limited Node.js access (only through preload scripts)
  //
  // If you see errors about missing Node.js APIs in renderer files, that's good!
  // It means TypeScript is protecting you from security vulnerabilities.

  "extends": "../../config/tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM"],
    "types": ["node"],
    "declaration": false,
    "declarationMap": false,
    "removeComments": true
  },
  "include": ["./**/*", "../shared/**/*", "../preload/**/*", "../types/**/*"],
  "exclude": [
    "../../node_modules",
    "../../dist",
    "../main",
    "../native",
    "./**/__tests__/**/*",
    "./**/*.test.ts",
    "./**/*.test.tsx"
  ]
}
